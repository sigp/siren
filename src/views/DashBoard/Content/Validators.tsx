import Typography from '../../../components/Typography/Typography'

const Validators = () => {
  return (
    <div className='w-full h-full flex items-center justify-center'>
      <Typography>Validators Components</Typography>
    </div>
  )
}

export default Validators
