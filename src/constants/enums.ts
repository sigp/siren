export enum UiMode {
    DARK = "DARK",
    LIGHT = "LIGHT"
}

export enum ContentView {
    DASHBOARD = "DASHBOARD",
    VALIDATORS = "VALIDATORS",
    LOGS = "LOGS",
    GRAFANA = "GRAFANA",
    SETTINGS = "SETTINGS"
}

export enum AppView {
    ONBOARD = "ONBOARD",
    DASHBOARD = "DASHBOARD"
}

export enum OnboardView {
    PROVIDER = "PROVIDER",
    CONFIGURE = "CONFIGURE",
    SETUP = "SETUP"
}