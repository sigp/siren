export enum UiMode {
    DARK = "DARK",
    LIGHT = "LIGHT"
}

export enum ContentView {
    DASHBOARD = "DASHBOARD",
    VALIDATORS = "VALIDATORS",
    LOGS = "LOGS",
    GRAFANA = "GRAFANA",
    SETTINGS = "SETTINGS"
}